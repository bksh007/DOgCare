<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bksh1.dogcare">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Clicker.ClickerActivity"></activity>
        <activity android:name=".DogBreedActivity" />
        <activity android:name=".ExpenseTracker.ExpenseActivity" />
        <activity android:name=".Appointment.AppointmentActivity" />
        <activity
            android:name=".ui.login.LogActivity"
            android:label="@string/title_activity_log" />
        <activity android:name=".LoginRegister.RegisterActivity" />
        <activity android:name=".LoginRegister.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>